global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: aria-middleware
    metrics_path: /metrics
    static_configs:
      - targets: ['middleware:3001']

  - job_name: aria-backend
    metrics_path: /metrics
    static_configs:
      - targets: ['backend:8080']
