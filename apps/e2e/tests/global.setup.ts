import { test as setup } from '@playwright/test';

setup('global setup', async () => {
  // Placeholder for any global test setup
  // e.g. seed test DB, create test user
  console.log('ARIA E2E: Global setup complete');
});
